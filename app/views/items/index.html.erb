<div class="container">
  <% if user_signed_in? %>
    <% @items.each do |i| %>
      <h2><%= link_to i.title, item_path(i) %></h2>
      <p><%= i.description %></p>
    <% end %>
  <% else %>
    <%= render 'welcome' %>
  <% end %>
</div>